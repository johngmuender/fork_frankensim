# GUM Program — Roadmap v5: the Theory-Audit Frontier

Successor to ROADMAP_v4_SCALE (executed). Phase H (this document's
Phase-A analogue) ran four parallel argument-level auditors over the
corpus's theory — the open NR gates, the ħ-closure's surviving pillars,
the never-audited gravitation/one-world sectors, and the constructive
repaired-closure question. Memos: `T1_NR_gates.md`,
`T2_closure_pillars.md`, `T3_repaired_closure.md`,
`T4_gravity_oneworld.md`; structured verdicts: `audit_summaries.json`.

## Phase H verdict census (48 load-bearing arguments audited)

| verdict | count |
|---|---|
| SOUND (valid given premises) | 19 |
| GAP (missing step, stated precisely) | 16 |
| DEFECT-CANDIDATE (step fails, with derivation) | 9 |
| NOT-AUDITABLE-FROM-TEXT | 4 |

## The theory ledger (defect-candidates, T-numbered; argument-level —
distinct from the arithmetic F-R ledger; promotable to F-R by the
numeric follow-ons of Phase H2)

- **T-H1 (NR-A2b ¾-Casimir)**: the calibration mixes operator
  normalizations across faces (2S/ħ vs S/ħ); every Wigner-Eckart-legal
  reading gives 3 or 1 — the printed 3/4 requires the mixed reading,
  and the 3/4 was inverted from data first.
- **T-H2 (NR-K3a second moment — potentially serious for the corpus's
  neutrino insert)**: the 0νββ insertion rate is quadratic in the
  amplitude; for line-supported density with a 2-fm kernel,
  ⟨|A|²⟩/|⟨A⟩|² ~ 10¹⁴–10¹⁶ — the "negligible" fluctuation term
  dominates by 14+ orders, in the OPPOSITE direction from the corpus's
  fork, leaving the four-cell collapse unsupported. Estimate-backed;
  the decisive Monte-Carlo is cheap (Phase H2.1).
- **T-H3 (closure-system circularity)**: IV.B/IV.H.1(iii) ↔ c″ ↔ T-B1
  premise loop identified.
- **T-H4 (c″ "identical dressed knots ARE fermions" as DF)**: the
  DF-forcing overreaches the FR-core's actual yield.
- **T-H5 (IV.D selection narrative)**: "deformation until κ<1 ⟹ in-gap
  derived" — the convergence story is asserted; the rejection of the
  rigid rung itself is SOUND.
- **T-H6 (VI.D α > 0 sign theorem)** and **T-H7 (the DESI
  direction-check sentence)**: the tracker identity
  w(z) = −1 + Ω_m(z) (proven, verified numerically) contradicts the
  printed direction claim as stated.
- **T-H8 (Lemma II.1/App F.1 pairs-only convexity)**.
- **T-H9 (C4)**: no natural functional recovers 𝔠₀ = 2.5147 — six
  steelman routes each fail; the zero-cost spheroidal reading
  reproduces it exactly but at an open endpoint whose zero-cost
  premise is refuted by the F-R4 value-function lemma.

Also: F-R4 cross-examination of NR-D2 is **direction-safe** (every
disposition strengthens the corpus's murk kill, margins ≥8 orders) —
the frozen-value lemma undermines the screening *theorem-status* while
rigorously closing the escape channel. And three new harmless print
slips (λ_dB, σ/m mass mismatch — F-R1-class, conservative).

## The constructive centerpiece (T3): the repaired closure is UNIQUE

**Theorem T3.1**: the stability-constrained closure (C2) coincides
EXACTLY with the saturated closure (C3) — the constrained optimum's KKT
point sits on the threshold boundary with zero multiplier; no
intermediate tuple exists. Combined with C1's honest costing (the
corpus's own spheroidal dial buys 5.6% of 𝔠, not 22.5%) and T-H9,
within the model there is one well-posed ħ-closure, in closed form:

| quantity | corpus (broken) | repaired (saturated) |
|---|---|---|
| 𝔠 | 128√42/105π = 2.5148 | **64√2/(9π) = 3.2011** |
| κ | √(7/12) = 0.7638 | **1/√2** |
| g | 3/2 (unreachable) | g_core = 35/24, g_tot = 35/12 |
| V = √2 | 1.4142 | support ratio **3/2** exactly |
| ω_th | √(7/3)ω₀ | **√2·ω₀** |
| S4′ | κ²g = 7/8 | **κ²g_tot = 35/24** |
| mass law | 𝔠Λm̃-class | +17.85% (5/(3√2)) |
| new identity | — | κ² = ê_tot/(2𝔦_tot); saturation ⇔ ê_tot = 𝔦_tot = 64/9π |

(κ²g = 7/8 is proven an exact identity on the whole locked-transport
family — so the corpus's S4′ bench stake cannot discriminate g.)
Caveats printed in the memo: marginal (not gapped) stability with a
degenerate halo manifold; the ε-law sign flips. The family sector is
𝔠₀-blind (P-O1 verified) — S1 unaffected either way.

## Phase H2 — the numeric follow-on program (ranked; top two EXECUTING)

1. **H2.1 (decisive, cheap) — NR-K3a second moment**: Monte-Carlo
   static line network × 2-fm double-propagator kernel;
   ⟨|A|²⟩/|⟨A⟩|² across (a, L, r) + time-averaged kernel to price
   motional narrowing. Settles T-H2. ▶ EXECUTING
2. **H2.2 — NR-D2 P4 two-texture relaxation** on fs-gum-field/statics:
   overlapping mismatched double-twist axes, full vs SDiff-projected
   moves; tests the F-R4 frozen-value floor directly on the corpus's
   collision problem. ▶ EXECUTING
3. H2.3 — NR-D1b Frank-energy anisotropy scan (radial vs hyperbolic
   hedgehog degeneracy; what reaches 8π/3).
4. H2.4 — A2b-1 helical counterexample (analytic + quadrature).
5. H2.5 — Seeley–DeWitt a₁ per sector (settles E.6 positivity).
6. H2.6 — the j(j+1)-convention non-rigid closure on fs-gum-statics.
7. H2.7 — entrainment ±½ empirical on the non-rigid solver; ⟨r2⟩-class
   locking-torque sign in the integrable limit (fs-gum-cosserat toy).

## Beyond (unscheduled)
Promotion pass (T-H → F-R where H2 numerics confirm); DISCHARGE_PACKAGE
v2 with the repaired-closure memo as the constructive companion; the
GPU-class physics unchanged from v4.
