# Tier 5a — Family/Neutrino/Color Arithmetic: Coordinator's Adjudication

Pilot: `family_RESULTS.md` / `family_audit.py` (deterministic). This tier
audits everything downstream of the printed Σ(p) integral values — the
"no fitted parameters" lepton-hierarchy chain, the neutrino inserts, and
the ⟨r11⟩ quark belts. The integrals themselves are not recoverable from
the text (stated precisely in the report) — this is the arithmetic and
error-propagation perimeter, Tier-0 style.

## Adjudicated verdicts

1. **The headline lepton numbers replicate.** Data-side logs exact
   (2.82239 / 5.33160 / 1.88903); the ½A and ½(A+B) pulls are 0.05σ and
   0.43σ as printed; the shape ratios (2.103 r7, 2.018 r8) check. The
   corpus's "≈2σ-equivalent / prior weight 2–4%" self-grade sits inside
   the bracket of reasonable statistics (1.3%–8.7% depending on the
   unprinted statistic) — a fair self-grade, convention-limited only.

2. **FINDING F-R6 (moderate, print-chain class — from T5-F1): the
   ⟨r7⟩ → E-F1 → ⟨r8⟩ narrative chain is arithmetically false as
   written.** 2.9 → exact ×2 → +14% cone stiffening = **6.61**, not the
   printed A = 5.6 (closure would need net −3.4%, more than 3× the
   printed ±5% away from +14%); and the printed errors *shrink* along
   the chain (±0.9 printed where the chain forces ≥ ±1.37) — impossible
   under an exact factor and uncertain corrections. All eight
   correction-ordering readings fail for A; B closes only marginally.
   The only coherent reading: **⟨r8⟩ is an independent re-evaluation**
   (implied leading values 2.46/3.05 — consistent with ⟨r7⟩ at
   0.74σ/0.17σ), and the prose presenting it as ⟨r7⟩×2+corrections is
   wrong. Direction-neutral: the honest chain value (6.61 → ½A = 3.31)
   would *worsen* agreement with data (~1.1σ) — so the defect is in the
   narrative, not a favorable-direction fudge, but under the corpus's
   own blind/escrow rules a result whose provenance sentence is false
   is exactly what its print-your-own-defects discipline exists to
   catch. Discharge: the corpus states ⟨r8⟩'s actual pipeline (fresh
   evaluation vs corrected ⟨r7⟩) and corrects §VII.C/K.1's sentence.

3. **FINDING F-R7 (minor — from T5-F2):** App K.2 prints "qR* ∼ 10⁻¹⁷"
   beside ln(1/qR*) = 24.36, which gives qR* = 2.6×10⁻¹¹ — six orders
   apart. The inversion side is internally coherent (implied
   R* = 1.11×10⁻¹⁶ m = the τ reduced Compton wavelength, exactly as the
   pipeline's base-m̃_τ convention implies), so the "10⁻¹⁷" is a stray
   magnitude in prose. No downstream number moves; S1 unaffected.

4. **FINDING F-R8 (minor — from T5-F3):** the locked-bond comparison's
   printed errors give a 1.54–1.59σ pull where the body claims 1.3σ and
   the corpus's own harmonization memo (F-A15-5) says 1.4σ; closing it
   needs an unprinted uncertainty on the 0.96 denominator. Grading
   language, not substance.

5. **The ⟨r11⟩ quark-belt sector is VINDICATED — the tier's positive
   surprise.** The pilot achieved a full reconstruction with nothing
   fitted: the spacing-ratio rule R = 1 + B_geo/A − B_tube/A reproduces
   the printed 1.27 ± 0.089 and 0.82 ± 0.161 to the error digits, the
   down-type sign flip is forced (0.33 − 0.51 = −0.18 < 0), the data
   side lands exactly from PDG with direct m_t = 172.5 GeV (and the
   MS-bar alternative measurably fails — pinning the corpus's scheme
   choice), and all four pulls land at 0.17–0.32σ against the claimed
   "0.2–0.3σ". Likewise C7: the "sinθ_c = 0.69 vs Cabibbo" worry
   dissolves — the corpus itself declares the symbol collision and
   seals the comparison (RK-7/H-K1), and sinθ_c = √(1 − 1/𝔪) = 0.688
   checks. ΔN_eff, the pitch, the termination arithmetic, ε_ν, and the
   bond-loop pull all replicate.

## Status

Tier 5a **complete**. The family sector's arithmetic perimeter is now
externally audited: its comparisons with data replicate cleanly
(including the quark-ratio reconstruction, which is stronger than the
corpus's own summary sentence), and the campaign ledger gains one
moderate print-chain defect (F-R6, provenance-sentence class) and two
minors (F-R7, F-R8). Nothing in this tier bears on the ħ-closure
findings (F-R4/F-R5) in either direction — the family sector hangs off
m̃-ratios, which are 𝔠₀-blind by the corpus's own Prediction P-O1
argument. What remains of Tier 5 (blue-fog condensate boxes, the
nucleation sector, the Σ(p) integrals from geometry) requires inputs or
compute outside this campaign's envelope and is documented as such.

Within-model throughout; nothing here bears on nature.
