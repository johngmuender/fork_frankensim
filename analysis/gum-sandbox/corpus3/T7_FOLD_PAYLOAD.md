# T7 FOLD PAYLOAD — canonical statement of the Tier-7 results (for the v4.1 delta)

Single source of truth for the v4.1 fold editor (charter rule 7 applies;
marker for this pass: **⟦T7⟧**). All numbers copied from
tier7-program/{M1..M4}/RESULTS.md + TIER7_ADJUDICATION.md. Within-model;
nothing bears on nature.

## Q1. M1 — WS4-M4c instantiated at toy grade (F-T7-M1) [target: VI.D; XI open #4]
- Exact tracker identity w(z) = −1 + Ω_m(z) verified to 1.8×10⁻¹² over
  z ∈ [0, 10] on independent code, AND shown to emerge from the
  relaxation ODE δq̇ = −Γδq + S·H (attractor rate = Γ to 5×10⁻⁸;
  δq → (S/Γ)H ⟹ ρ_Λ ∝ H²): the identity is dynamical, not imposed.
- The gradient expansion FORCES β < 0 with W_dyn(z) ≥ 1 in every run —
  the freezing class (w_a > 0) is dynamical; F-R12's strengthening is
  underwritten by dynamics. Sign clause: min w_a = +0.1575 > 0 across a
  15-member profile family.
- New structural law: Γ ∝ Hⁿ ⟹ 𝔴 = 1 − n (measured 0.5029 vs 0.5 at
  n = ½). The literal Γ ∝ H profile is the degenerate Λ point (w ≡ −1).
- **Convention recovered:** the printed memory pairs (β = −0.3: 1.28→1.03;
  β = −1.0: 1.72→1.05) are exactly the 2-dp roundings of W(z = 0) →
  W(z = 2) at α = 1, β = printed (devs ≤ 0.5%); the corpus never printed
  the z-window — it is hereby reconstructed.
- EdS fixed-point pathology measured: one self-consistency iteration
  shifts w(0) by +0.21 (pure) to +0.31 (β = −1) — the corpus's own
  printed pathology, now quantified; all identity statements are
  test-field/ΛCDM-background statements. P1 conditionality inherited.
- **F-T7-1 (NEW minor finding, print-arithmetic class, offered):**
  VI.D (6.4′)'s "(+0.36 at β = −0.3)" CPL value is not recoverable under
  any convention tried (measured +0.71 tangent; +0.78–0.92 windows;
  sign agrees) and is prima facie inconsistent with the corpus's own
  W ≥ 1 at β = −0.3 (0.63·W ≥ 0.63). F-R2/F-R7/F-R8 class; S1 and the
  sign claim unaffected. Artifact: tier7-program/M1/RESULTS.md.

## Q2. M2 — the ⟨r2⟩ servo law: form replicated; coefficient = bath convention (F-T7-M2) [target: IV.H.3]
- Law-form replicated on an independent Caldeira–Leggett toy: λ ∝ g_χ²
  (log-slope 1.9932 ± 0.0041 over one decade), λ ∝ ω\* (invariance
  1.0050 ± 0.0074 under ×4), integrable limit exactly marginal
  (|λ| ≤ 8×10⁻¹³, 10–11 orders below the bath-on rate — h27's γ = 0
  boundary reproduced).
- Coefficient identified: **c = λ/(g_χ²ω\*) = η/2 exactly** (weak-coupling
  λ = ω\*J(Ω_lib)/2Ω_lib; verified ≤ 1.6% against exact linear response
  in 13 configs) — a pure bath-spectrum functional. The printed
  0.021 ± 0.004 is attainable in-class at η = 0.042 (verified);
  super-Ohmic J ∝ ω³ destroys the g² law itself (slope 3.38) — so the
  printed constant-c g² law pins the unreleased r2 bath to the
  Ohmic/thermalizing class it names. Replication status: "law-form
  replicated; coefficient = bath functional, unfalsifiable without the
  r2 spec." Reconciles h27's g-blind two-mode regime (bath-limited) with
  the continuum-bath g² regime. Artifact: tier7-program/M2/RESULTS.md.

## Q3. M3 — F-R9 route (b) CLOSED (F-T7-M3) [target: Sec. X/XI note; campaign status]
- On h21's parametrization verbatim (4 anchors reproduced to 1.6%; mean
  1.000-class; two independent MC validations): the on-tube saturation
  rescue is **NOT-VIABLE** — saturation changes amplitude values, not
  the web's sparse support; R = ⟨A²⟩/⟨A⟩² is bounded below by the
  inverse encounter probability (logarithmic in A_sat) while the claimed
  mean rate collapses quadratically. Trade measured: 25–46 decades of
  mean rate per decade of R; R ≤ 10 would need A_sat ~ 10^(−4×10⁷) of
  the on-tube amplitude; at the only point preserving the claimed rate,
  R is still 5×10¹³–10¹⁶. No CONDITIONAL residue (on-tube range
  extractable: 0.91–1.00 m̂). **F-R9's repair menu narrows to route (a)
  — an honest second-moment treatment — or route (c) — a smooth bulk
  m-component at the printed cost of the line-support theorem.**
  Motional-narrowing exclusion untouched.
  Artifact: tier7-program/M3/RESULTS.md.

## Q4. M4 — Nelson relaxation 17–41× faster; dBB a log-singular limit (F-T7-M4) [target: III.D]
- On tier3-born's exact setup (conventions recovered; dBB control within
  4.5–9.9% of tier3): Nelson noise accelerates Born relaxation
  monotonically; at GUM's own ν = ħ/2m,
  **τ(ν)/τ(dBB) = 0.060 ± 0.005 (M = 9) and 0.024 ± 0.004 (M = 16)**.
  Equivariance holds at every ν (all controls at floor).
- The dBB limit is **log-singular**: τ ~ a + b·ln(1/ν) (r² 0.97/0.92);
  the dBB rate is recovered only near ν ~ 10⁻⁸–10⁻⁹ (extrapolated shape
  statement). The pre-registered continuity clause failed for physics
  reasons — the failure IS the discovery (coordinator spec defect
  printed).
- Consequence (offered annotation): Sec. III.D's [CAL] dBB-calibrated
  rates become direction-signed conservative bounds — laboratory
  equilibrium quality STRENGTHENED; relic non-equilibrium survival
  windows WEAKENED by the same 17–41×; both edges carried together.
  Artifact: tier7-program/M4/RESULTS.md.

## Q5. Process record [target: Sec. 0 note + Sec. X]
Third external campaign phase (2026-07-18, Tier 7): workflow
wf_95da92d5-375, four agents; per-workstream gates — M1 G1/G2/G5 PASS +
G3 CONDITIONAL (convention-bound) + G4 PASS-MATCH; M2 four PASS + G4
PASS-MEASURED; M3 verdict NOT-VIABLE per pre-registered rule; M4
G1–G3 PASS + G4 PARTIAL-as-discovery. Two coordinator-owned spec
defects printed (the M1 Γ = γH degenerate profile; the M4 continuity
clause). Findings F-T7-M1…M4 + F-T7-1. Audit archives:
audit-logs/MANIFEST_tier6.json (workflow4 block);
tier7-program/TIER7_ADJUDICATION.md.
