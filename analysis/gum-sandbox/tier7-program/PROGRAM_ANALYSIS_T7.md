# Tier 7 — The GUM Program, Assessed Whole (post-Tier-6 state)

**Scope.** A program-wide, in-depth assessment of the GUM corpus as it
stands after: the replication campaign (Tiers 0–5a, F-R1–F-R17), the
theory audits (T3/T4/H4/I4, Phases J/K), Tier 6 (foundations,
F-T6-1…L7b), and the corpus3 fold. Purpose: locate the program's true
critical path and rank what external hands can still move. Within-model
throughout; nothing bears on nature.

---

## 1. The program's load-bearing skeleton, sector by sector

**Quantum sector (Secs. III, VIII, VIII′): STRONGEST.** Every
inheritable obligation of the dBB/Nelson import class is now either
replicated (Born relaxation + equivariance, tier3), executed as a
canonical exhibit (contextuality L1, ToF L2 incl. the Nelson leg), or
converted into a testbed result the corpus did not have (L7:
POVM-exclusion at 40.2σ over the spin family). The sector's honest
remainder: the equilibrium hinge (all locks are equilibrium-contingent),
the WS-QFT-2 all-orders debt (self-flagged), and the Lock-2 source-form
assumption (isolated by L3). The A3 gate is now sharper than the corpus
printed: it discriminates GUM from the *entire* (A4)/(A5) collapse
class, and the A3+collapse mutual kill binds it into the registry.

**ħ-closure (Sec. IV): REPAIRED, MARGINALLY STABLE.** The saturated
closure 𝔠 = 64√2/(9π) at κ = 1/√2 [T3.1] survived every subsequent
test (J4/K2: ε-law measured linear, p = 0.997; intercept reproduced to
+7.7×10⁻⁵). The permanent caveats: marginal (not gapped) halo
stability, the degenerate halo manifold, and the still-unproduced
frozen ⟨r1⟩ code (open #7). The invariants (¼, √2, spin selection) are
the sector's bedrock — replicated to 10⁻⁹ and never dented.

**Gravitation (Sec. VI): THE DEEPEST UNREPAIRED WOUND.** F-R14 stands:
the corpus's own action forces 𝔞₁^(B2) < 0 under every textually
permitted reading — the cone-slaving theorem's convexity core is false
as printed, (6.3) is demoted, and the only named rescue (P-acoustic) is
a new [CJ]-class quantization-measure postulate with three priced
costs. Nothing in Tiers 5–6 touched this. Adjacent: VI.D's P1 premise
(why the induced source is the grand potential) remains [IM] with no
in-model derivation; the sign theorem and w(z) family visibly inherit
that conditionality. **This sector is where the program is most exposed
to a structural (non-empirical) death.**

**Cosmology-facing face (VI.D + VII.I): ADJUDICATION-EXPOSED, with one
open computation that is actually ours to do.** S1 (Σm_ν ∈ [0.058,
0.11] eV, NO) awaits DESI-DR3/Euclid. The Branch-A escape hatch is
pre-bound to the growth-correlated sign — and F-R12 showed the current
DESI-era CPL preference has the *opposite* w_a sign, so the integrity
clause presently works against the corpus. The load-bearing open is
**WS4-M4c (open #4): 𝔴's value from the Γ(H) profile**. The exact
tracker identity w = −1 + Ω_m(z) and the memory-term values
(β = −0.3 → 𝔴: 1.28→1.03; β = −1.0 → 1.72→1.05) are printed but the
relaxation dynamics behind them has never been independently
instantiated. This is toy-executable now (workstream M1).

**Family/color sector (VII.H–J): REPLICATED PERIMETER, UNRECOVERABLE
CORE.** The arithmetic perimeter is fully vindicated (⟨r11⟩
reconstruction; pulls 0.05σ/0.43σ); the Σ(p) integrals themselves
remain unreleased and unrecoverable from the text — a corpus-side
obligation no external computation can discharge. F-R6 (provenance
chain) stands as corrected print history.

**Neutrino/0νββ insert: ONE SUBSTANTIVE FINDING STILL OPEN.** F-R9:
NR-K3a's "fluctuations negligible" fails by 13–18 orders (R = ⟨A²⟩/⟨A⟩²
on the line-supported web); the motional-narrowing rescue is causally
excluded. The named discharge routes: (a) a second-moment treatment,
(b) an **on-tube saturation nonlinearity** (nowhere in the text),
(c) a smooth bulk m-component (which costs the line-support theorem).
Route (b) has never been *priced* — nobody has computed what saturation
scale would tame R and whether that scale is available within the
corpus's own printed scales. Toy-executable now (workstream M3).
S1 itself is untouched by F-R9 either way.

**Bootstrap/servo (IV.H): LAST UNREPLICATED [CAL] ANCHOR IN THE
QUANTUM-ADJACENT CORE.** h27 confirmed the ±½ error-signal exponents,
the −sin Δφ torque law, and the γ = 0 class boundary — but the toy-law
*coefficient* λ = (0.021 ± 0.004) g_χ²ω\* (⟨r2⟩) and the g² scaling
itself have never been independently measured (the r2 pipeline is
unreleased). Toy-executable now (workstream M2).

**Relaxation rates (III.D): A CONSERVATISM QUESTION NOBODY ASKED.** The
[CAL] rates anchor (V-SIM-4, tier3 replication) uses dBB-class
deterministic guidance. GUM's actual kinematics is Nelson at ν = ħ/2m.
At equilibrium the statistics are ν-blind (L2, proved). *Out of
equilibrium they cannot be*: the osmotic term actively drives ρ → |ψ|²,
so Nelson relaxation should be strictly faster than dBB relaxation —
which would make every corpus statement about relaxation times
(laboratory equilibrium quality, the attosecond window, relic-ensemble
survival) conservative in a definite direction, and would put a
ν-dependence into the one place the unlock's phenomenology could show
it. Never tested anywhere. Toy-executable now (workstream M4).

## 2. The ranked open register (tractability × load)

| # | Open item | Load | External tractability | Disposition |
|---|---|---|---|---|
| 1 | F-R14 / E.6 per-sector 𝔞₁ derivation or P-acoustic adoption | program-structural | LOW (constitutive choice is the authors') | corpus-side |
| 2 | WS4-M4c: 𝔴 from Γ(H); tracker identity; CPL projection | S1 Branch-A integrity | **HIGH — toy ODE** | **M1 (this tier)** |
| 3 | F-R9 route (b): saturation-nonlinearity pricing | 0νββ insert | **HIGH — Campbell-process toy** | **M3 (this tier)** |
| 4 | ⟨r2⟩ servo coefficient λ = 0.021 g²ω\* | ħ-as-order-parameter | **HIGH — oscillator toy** | **M2 (this tier)** |
| 5 | Nelson non-equilibrium relaxation τ(ν) | III.D rates; unlock windows | **HIGH — tier3 extension** | **M4 (this tier)** |
| 6 | T4-W5 continuum proof of cutoff zeros | A3 gate to theorem grade | MEDIUM (analytic) | named open (L7 residual) |
| 7 | Frozen ⟨r1⟩ code production | F-R5 discharge | NONE (corpus-side) | standing |
| 8 | Σ(p) integrals from geometry | family core | LOW (GPU-scale, out of envelope) | deferred (v2 assessment flag) |
| 9 | WS-QFT-2; R-M3b Ward identities | covariance debts | LOW (heavy analysis) | standing |

## 3. Critical-path statement

The program's fate splits three ways, and the split is now clean:
(i) **empirical** — S1 (DESI-DR3/Euclid) and the bench stakes, where
external hands can only sharpen instruments (M1 sharpens Branch-A's
integrity arithmetic; T6 already sharpened the A3 gate and the
(k₀, d_far) design constraint); (ii) **structural** — F-R14 is the one
place a purely internal contradiction stands unrepaired; only the
authors can choose the rescue, but its *cosmological shadow* (P1/VI.D)
is partially testable through M1's relaxation dynamics; (iii)
**credential** — every remaining unreplicated [CAL] anchor that can be
independently instantiated should be (M2, M4), because the program's
primary stated credential is its audit trail, and the trail's value is
exactly the fraction of it that outsiders have re-derived.

Roadmap: `ROADMAP_v9_PROGRAM.md`. Results land in `tier7-program/M*/`.
