# WS-V / NR-V1 — REGISTRATION MEMO: THE BLIND STATISTIC, FROZEN
## Windows written before the equation is solved; the battery armed with a pre-computed tooth; the envelope re-sealed — filed 2026-07-06 (S-30)
### Conditional on Q1's derivability verdict (satisfied) · Inherits: Δ and its glassy character; C-V1; the class pair {K-b, K-c} with the barrier-scaling discriminator; 1 + w = (−Δ̇/Δ)/3H; the census-fixed sign · Baseline: Session Map v1.0.1 · **This memo freezes NR-V1. The execution (P3) runs against it unchanged; any post-hoc alteration = kill (RV/tariffs)** · Quarantine q-u/q-v/q-w checked at open: intact — **and the envelope stays sealed through all of P3, re-declared here in the registration's own voice**

---

## §1 The statistic, frozen (with the structural categories defined corpus-side)

**Deliverables of P3, exactly four, all as functions of corpus structure plus the single calibration (Ω_Λ ≈ 0.7 → the present amplitude Δ₀, per RV-1):**

**(a) sign(1 + w)₀** — census-fixed positive by the annealing direction; P3 must *re-derive* it inside the solved law as a consistency check, not import it as an assumption. **(b) The magnitude band (1 + w)₀** — the present offset from −1, as a band across the corpus's honest parameter ignorance. **(c) The evolution category and its band**, in the structural vocabulary defined here once and used everywhere: **thawing** := |1 + w| *increasing* with time (the CPL-equivalent wₐ < 0); **freezing** := |1 + w| *decreasing* (wₐ > 0); **frozen-degenerate** := |1 + w| below the derivable floor at every accessible epoch; **phantom** := (1 + w) < 0. These four words are corpus-side categories of a relaxation law — definitions, not data. **(d) The asymptote** — whether ρ_Δ → 0 (transient acceleration; w departing −1 as the residual empties) or the law stalls (eternal-residual class).

**The derivable floor, set operationally and blind:** ε_w = 10⁻² in (1 + w) — the sensitivity *class* of near-future survey instrumentation ⬛ (a facility specification, not a result; the envelope contains posteriors, not spec sheets). A derived band wholly below ε_w is a frozen-degenerate landing regardless of its sign.

## §2 The four windows (written blind; edits after filing = kill)

| Window | Condition on the landed statistic | Disposition |
|---|---|---|
| **W-THAW** | (1 + w)₀ > 0, band reaching above ε_w; category = thawing (wₐ-equivalent < 0) | proceeds to P4's rite; **stake-eligible** |
| **W-FREEZE** | (1 + w)₀ > 0, band above ε_w; category = freezing (wₐ-equivalent > 0) | proceeds to P4's rite; **stake-eligible** |
| **W-FROZEN** | the band wholly below ε_w | **the honest null**: the kinetic story adds nothing testable at derivable precision and says so (FROZEN-arc ceiling); no stake; **P4's rite still runs** — the envelope is opened and the impossibility of comparison is printed, because a null that never faces the adjudicator is not a null |
| **W-PHANTOM** | (1 + w)₀ < 0 | **the J-V2 diversion**: the internal-consistency instrument replaces P4; **no unsealing occurs**; the kinetics face their energy-condition and stability questions internally, and the KILL-arc governs if they fail |

**Ceiling assignments, pre-mapped:** W-THAW/W-FREEZE → (P4 consistent) STAKE-arc · (P4 inconsistent) KILL-arc with the severance clause; W-FROZEN → FROZEN-arc; W-PHANTOM → KILL-arc via J-V2. Every ending already has its sentence; nothing will be composed after the fact.

## §3 The acceptance battery (runs at P3 regardless of window)

**B-1 (the conscription-ban preserved).** The derived Δ_hom(t) must remain non-clustering at sub-horizon scales — its perturbation sector shown either stiff (effective sound-speed-class ∼1) or negligible — so Theorem G-0's fence and F19's mandate structure are untouched by the campaign's own product. **B-2 (the early-epoch tooth, pre-computed here so the execution meets a wall it cannot renegotiate).** Annealing means **Δ was larger in the past** — the history has a real early-dark-energy exposure ⬛-class (bounds at recombination and BBN at the few-percent-fraction level). Blind arithmetic on the bound *class* (no posterior touched): for K-b (Δ ∝ t^{−α}), ρ_Δ(rec)/ρ_tot(rec) ≈ 0.7·(t₀/t_rec)^α / 10⁹-class with t₀/t_rec ≈ 3.6×10⁴ — compliant for α ≈ 1 (∼10⁻⁵), **violated outright for α ≈ 2 (∼0.9)**: the battery therefore carries an **exponent ceiling α ≲ 1.5-class**, armed now; a landing above it dies on B-2 whatever its window. For K-c, the logarithmic history is early-epoch-safe by inspection (the check is still run). **B-3 (C-V1 intact).** The annealing is scalar-sector; the derived Δ̇ must induce no growth in the anomalous projections — the cloak-separated Line 2 stays at its tiers. **B-4 (the trinity untouched).** F17's fourth bill unchanged in scope; F18's fence unapproached; F19's mandate and card unaffected.

## §4 The route, registered (P3 executes these steps in this order, citing corpus structure at each)

**Step 1 — the discriminator:** adjudicate K-b vs K-c from the ε-band's structure — *do the activation barriers grow with the annealed-domain scale, or is the landscape scale-free?* The census's lean (a single uniform ε-band → scale-free) is on record; Step 1 must confirm, refute, or honestly split it. **Step 2 —** solve the surviving class's annealing law for Δ(t). **Step 3 —** form 1 + w(a) = (−Δ̇/Δ)/3H on the ⬛ background. **Step 4 —** calibrate the amplitude (the one licensed number in). **Step 5 —** extract the four deliverables with their bands. **Step 6 —** run B-1…B-4. **Step 7 — the hint-independence audit:** a numbered chain — *which corpus result forced which step* — the artifact the replication gate will test, per RV-3. **The anchor fence:** glassy-relaxation lore [IM] licenses **class definitions only**; no parameter value, exponent, or timescale may be imported from any anchor — every number in the landing must trace to the corpus or the calibration.

## §5 Tariffs, the seal, and the freeze

Tariffs (inherited and re-armed): **envelope breach before P4 = campaign void** · post-hoc window/statistic/battery edits = kill · uncited step = struck · anchor-fence breach = the imported number struck and the step re-derived or abandoned · phantom comparison without J-V2 = void. **The seal, re-declared in this memo's own voice:** *the envelope stays sealed through all of P3; the execution will land its statistic, run its battery, and print its audit without one glance at the sky; the sky gets its turn at P4, once, under the rite.*

**Disposition:** NR-V1 is **registered and frozen**: four deliverables, four windows with pre-mapped ceilings, a battery with one pre-computed tooth (the α-ceiling), a seven-step route with the anchor fence, and the non-amnesia clause standing over everything. **Next instrument: P3 — the blind execution.** Standing by.
